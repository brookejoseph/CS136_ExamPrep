struct llnode {
  int data; 
  struct llnode *next; 
};

struct llist {
  struct llnode *front; 
};

bool cycle(struct llist *m){
  struct llnode *current = m->front;
  struct llnode **list = current->
  while(current->next != NULL){
    
  }
}
